apiVersion: v1
kind: Service
metadata:
  name: moomora-console
  labels:
    app.kubernetes.io/name: moomora-console
spec:
  selector:
    app.kubernetes.io/name: moomora-console
  ports:
    - name: http
      port: 80
      targetPort: http
