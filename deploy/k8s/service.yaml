apiVersion: v1
kind: Service
metadata:
  name: taskboard
  labels:
    app.kubernetes.io/name: taskboard
spec:
  selector:
    app.kubernetes.io/name: taskboard
  ports:
    - name: http
      port: 80
      targetPort: http
